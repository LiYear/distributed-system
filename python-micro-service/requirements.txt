# ============================================
# 合并依赖 — Dockerfile 构建用（4 个微服务的依赖合集）
# ============================================

# Web 框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# 数据库
sqlalchemy[asyncio]>=2.0.23
# 异步 MySQL 驱动（SQLAlchemy async engine 需要）
aiomysql>=0.2.0
pymysql>=1.1.0

# 缓存（async Redis 客户端 — redis[asyncio] 兼容 Python 3.11+）
redis>=5.0.0

# 消息队列（async AMQP 客户端）
aio-pika>=9.0.0
pika>=1.3.0

# 服务注册
nacos-sdk-python==2.0.11

# 数据校验
pydantic>=2.4.2

# HTTP 客户端
httpx>=0.25.0

# 认证
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1

# 配置管理
python-dotenv>=1.0.0
